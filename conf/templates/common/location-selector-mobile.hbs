<!-- start common/location-selector-mobile.hbs -->
<div class="location-dropdown-xs">
{{#withLanguageOptions}}
{{#if this.[1]}}
  <form id="form-select-language-mobile" action="/language" method="POST">
    <input type="hidden" name="csrfToken" value="{{csrfToken}}"/>
    <ul>
      <li>
        <select name="language" id="language-select-mobile" class="select location-select">
          {{#each this}}
          <option value="{{value}}" {{#if selected}} selected {{/if}}>{{label}}</option>
          {{/each}}
        </select>
      </li>
    </ul>
  </form>
{{/if}}
{{/withLanguageOptions}}
{{#withCountryOptions}}
{{#if this.[1]}}
  <form id="form-select-country-mobile" action="/country" method="POST">
    <input type="hidden" name="csrfToken" value="{{csrfToken}}"/>
    <ul>
      <li>
        <select name="country" id="country-select-mobile" class="select location-select">
          {{#each this}}
          <option value="{{value}}" {{#if selected}}selected{{/if}}>{{label}}</option>
          {{/each}}
        </select>
      </li>
    </ul>
  </form>
{{/if}}
{{/withCountryOptions}}
</div>
<!-- end common/location-selector-mobile.hbs -->
